{
	"name": "node-ocr",
	"license": "UNLICENSED",
	"scripts": {
		"server": "pm2 stop init && pm2 delete init && pm2 start init.js --ignore-watch='.git/* storage/*' --watch && pm2 logs"
	},
	"dependencies": {
		"await-exec": "~0.1.2",
		"axios": "~0.24.0",
		"express": "~4.17.1",
		"joi": "^17.8.3",
		"pdf-parse": "^1.1.1",
		"pdf-to-text": "~0.0.7",
		"uuid": "^9.0.0"
	},
	"devDependencies": {
		"nodemon": "^1.19.4"
	}
}
